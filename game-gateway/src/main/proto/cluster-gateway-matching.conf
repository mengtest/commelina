# 自定义配置
include "cluster-matching-common"

akka {
  actor {
    provider = remote
    allow-java-serialization = off
  }
  remote {
    enabled-transports = ["akka.remote.netty.tcp"]
    netty.tcp {
      hostname = "127.0.0.1"
      port = 3004
    }
 }
}