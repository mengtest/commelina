syntax = "proto3";

package def.constants;

option java_package = "com.game.room.constants";
option java_outer_classname = "ConstantsDef";

enum ERRORCODE_CONSTANTS {
    EMPTY1 = 0;
}

enum OPCODE_CONSTANTS {
    // 成功加入匹配
    JOIN_MATCH_RESPONSE = 0;
    // 通知匹配成功
    NOTIFY_MATCH_SUCCESS = 1;

    REMOTE_MATCH_RESPONSE = 2;
}