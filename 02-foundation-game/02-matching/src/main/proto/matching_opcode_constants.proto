syntax = "proto3";

package def.opcode_constants;

option java_package = "com.module.foundation.game.matching.constants";
option java_outer_classname = "OpcodeConstantsDef";
option java_multiple_files = true;

enum OPCODE_CONSTANTS {
    // 成功加入匹配
    JOIN_MATCH_RESPONSE = 0;
    // 通知匹配成功
    NOTIFY_MATCH_SUCCESS = 1;

    REMOTE_MATCH_RESPONSE = 2;
}